import 'package:flutter/material.dart';

class LimitedAccess extends StatelessWidget {
  const LimitedAccess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LIMITED ACCESS'),
    );
  }
}
