import 'package:flutter/material.dart';

class RestrictedAccess extends StatelessWidget {
  const RestrictedAccess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RESTRICTED ACCESS'),
    );
  }
}
