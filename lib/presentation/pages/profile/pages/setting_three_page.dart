import 'package:flutter/material.dart';

class SettingsThreePage extends StatelessWidget {
  const SettingsThreePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Settings Page Three'),
      ),
    );
  }
}
