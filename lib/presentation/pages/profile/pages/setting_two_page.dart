import 'package:flutter/material.dart';

class SettingsTwoPage extends StatelessWidget {
  const SettingsTwoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Settings Page Two'),
      ),
    );
  }
}
