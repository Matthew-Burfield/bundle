// import 'package:bundle/infrastructure/core/device_permissions_facade.dart';
import 'package:flutter_test/flutter_test.dart';
// import 'package:mockito/annotations.dart';
// import 'package:permission_handler/permission_handler.dart';

// import 'device_permission_test.mocks.dart';

// @GenerateMocks([Permission])
void main() {
  // late DevicePermissionsFacade devicePermission;
  // late MockPermission mockPermission;

  setUp(() {
    // mockPermission = MockPermission();
    // devicePermission = DevicePermissionsFacade(MockPermission);
  });

  group('Storage permissions', () {
    test('Should return true if the storage permissions are allowed.', () {
      // arrange

      // act
      // final result = devicePermission.hasStoragePermission();

      // assert
      // verify();
      // expect(result, true);
      expect(true, true);
    });
  });
}
